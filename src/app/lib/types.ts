export interface Camera {
  id: string;
  name: string;
  ip: string;
  address: string;
  coordinates: string;
  status: string;
  onoff: boolean;
}

export interface AiBox {
  id: string;
  name: string;
  ip: string;
  address: string;
  coordinates: string;
  status: string;
  onoff: boolean;
}
